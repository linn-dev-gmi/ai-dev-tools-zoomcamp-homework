[project]
name = "shortlink"
version = "0.1.0"
description = "Multitenant URL shortener with click analytics - AI Dev Tools Zoomcamp homework 1"
requires-python = ">=3.12"
dependencies = [
    "django>=6.1",
    "pymongo>=4.18",
    "python-dotenv>=1.0",
]
